/**
 * @file
 *
 * @see {@link https://developer.mozilla.org/docs/Web/JavaScript/Reference/Global_Objects JavaScript 标准内置对象}
 *
 * @version 2019-11-24
 * @since 2019-11-24
 * @author zhengrr
 * @license Unlicense
 */

'use strict'

/**
 * 函数属性
 * 全局函数可以直接调用，不需要在调用时指定所属对象，执行结束后会将结果直接返回给调用者。
 */
test('function properties', () => {

})

/**
 * 基本对象
 * 顾名思义，基本对象是定义或使用其他对象的基础。基本对象包括一般对象、函数对象和错误对象。
 */
test('fundamental objects', () => {

})

/**
 * 数字和日期对象
 * 用来表示数字、日期和执行数学计算的对象。
 */
test('numbers and dates', () => {

})

/**
 * 字符串
 * 用来表示和操作字符串的对象。
 */
test('text processing', () => {

})

/**
 * 可索引的集合对象
 * 这些对象表示按照索引值来排序的数据集合，包括数组和类型数组，以及类数组结构的对象。
 */
test('indexed collections', () => {

})

/**
 * 使用键的集合对象
 * 这些集合对象在存储数据时会使用到键，支持按照插入顺序来迭代元素。
 */
test('keyed collections', () => {

})

/**
 * 结构化数据
 * 这些对象用来表示和操作结构化的缓冲区数据，或使用 JSON（JavaScript Object Notation）编码的数据。
 */
test('structured data', () => {

})

/**
 * 控制抽象对象
 */
test('control abstraction objects', () => {

})

/**
 * 反射
 */
test('reflection', () => {

})

/**
 * 国际化
 * 为了支持多语言处理而加入 ECMAScript 的对象。
 */
test('internationalization', () => {

})

/**
 * WebAssembly
 */
test('WebAssembly', () => {

})

/**
 * 其他
 */
test('other', () => {

})
